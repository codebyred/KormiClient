export type workerBooking = {
    id: number,
    clientId:number,
    workerId:number,
    status:string
}

export type serviceBooking = {
    id:number,
    clientId:number,
    serviceId:number,
    status:string
}